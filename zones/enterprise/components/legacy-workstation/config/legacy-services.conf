# UU P&L Hex Computing Division -- Legacy Service Configuration
# Generated: 2001-09-03 by wsadmin
# Modified:  2003-11-04 (hostname change)
# Modified:  2006-08-22 (added SSH)

[hostname]
previous = HEXLEG-WS1
current  = hex-legacy-1

[network]
ip       = 10.10.1.10
gateway  = 10.10.1.1
domain   = uupl.local

[services]
smb     = enabled  ; UUPL workgroup, NT1 max, LAN Manager auth on
ftp     = enabled  ; anonymous read-only, anon_root=/srv/smb/public
telnet  = enabled  ; xinetd -- do not disable (remote maintenance)
ssh     = enabled  ; added 2006, PasswordAuthentication yes, PermitRootLogin yes

[shares]
public  = /srv/smb/public   ; guest ok, read only
private = /srv/smb/private  ; Administrator only

[accounts]
; See smbpasswd for hashed versions
Administrator = hex123
root          = hex123
